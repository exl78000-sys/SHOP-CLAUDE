# GONY 西裝店管理系統 — 專案交接文件

> 這份文件給 Claude Code，說明專案全貌、商業邏輯、技術架構與待辦。請先完整讀過。
> **每次開工先讀 `WORKLOG.md`**：最新工作流程、踩坑注意事項、系統現況與待辦都在那裡；完成工作後也要寫回 WORKLOG.md。

## 專案概述

這是一間客製西裝店（GONY）的訂單管理系統。店主 Eddie 用繁體中文溝通、沒有程式背景。系統前端 Vite + React，後端 Vercel Serverless Functions，資料存在 Notion。操作以手機/平板為主，介面採暗色系（深藍底 #0F1923 + 金色 #C9A84C）。

開發溝通請用繁體中文，避免艱深術語，步驟要具體。

## 技術架構

```
Vite + React（前端，HashRouter 路由）
  src/main.jsx          入口 + 導航 + 4 個分頁
  src/pages/
    OrderEntry.jsx      訂單系統（4 步驟：客戶→樣式→量身→定價）
    Dispatch.jsx        派工管理（建立 7 種派工單）
    DispatchTracking.jsx 派工追蹤（狀態、完成、工資）
    QuickExpense.jsx    快速記帳

Vercel Serverless（後端 /api）
  _notion.js          Notion REST 共用工具（DB id、property builder）
  _styles.js          樣式文字格式化 + 工資計算
  create-order.js     建訂單 + 找/建客戶 + 量身 + 自動財務收入
  orders.js           搜尋訂單
  create-dispatch.js  建派工單
  dispatches.js       列出派工單
  update-dispatch.js  更新派工狀態/工資/送回日期
  expense.js          記帳 + 產生本月固定成本

Notion（資料庫）
  環境變數 NOTION_TOKEN（Integration token）
```

## Notion 資料庫 ID

- 客戶資料庫：0bb6a9cb-5c82-44ab-b4af-6e461695f002
- 訂單資料庫：98f18257-9c28-41a5-a676-31bebb23e769
- 量身資料庫：1bfae58f-449d-47fb-a808-3f316e266ebf
- 派工單資料庫：e6dbceba-5e2a-4bb0-97d8-623622f515fb
- 財務總表：06c95176-c04c-4a2d-9428-4080d5bd5317

這些資料庫必須先分享給 Notion Integration 才能讀寫。

## 核心商業邏輯（定價）

售價公式：`售價 =（工本費 + 布料 + 布損）×（1+利潤率）`，進位到百元。

五品項工本費：二件式 32500、三件式 35450、外套單件 24300、褲子單件 7300、襯衫 1400、背心 7300。

預設碼數：二件式 3.2、三件式 4.0、外套 2.5、褲子 1.4、背心 1.0、襯衫 1.7。布料總價 = 單碼價 × 碼數。

布損級距（襯衫不適用）：≤4000→0、≤6000→1000、≤10000→2500、≤16000→4000、≤20000→5000、≤26000→6500、>26000→7000。

利潤率（一般品項，依布料總價）：≤4000→0%、≤10000→10%、≤16000→15%、≤20000→20%、≤26000→25%、>26000→30%。
利潤率（襯衫）：≤800→55%、≤1500→70%、≤2000→75%、≤2500→80%、>2500→85%。

二件式售價拆分：外套 3/4、褲子 1/4（僅顯示用，師傅工資另計）。

## 師傅工資（論件月結，完成後才確認，可手動微調）

計算邏輯有兩套並存：訂單建立時 `api/_styles.js` 的 `calcWages()` 自動算派工單工資（從樣式選項推算）；
另有獨立的 `/wage-calc` 頁面供手動輸入試算（不連訂單，各師傅分開記錄結算，見下方「工資試算頁」）。

- **外套師傅**：底價 7000
  - 扣眼 80/顆：前身扣眼數＝單排時取「排扣扣數」欄位（預設 2）、雙排固定 3 顆；
    另加 袖口扣數×2（雙手）——例：袖 3 扣＝6 顆、袖 4 扣＝8 顆
  - 雙排釦 +600
  - 米蘭眼 100/顆（通常 1 顆）
  - 票帶 +100、劍領 +300、半裡 +300、全單 +600、大衣 +1500
  - 特殊工資自填（金額直接加總，不分項）
- **褲子師傅**：固定 1900 +特殊自填
- **背心**：褲子師傅承接固定 1900；外套師傅承接 單排 2200 / 雙排 2500
- **經理（含打板）**：經理費(二件 2000/外套 1600/褲子 400/三件 2400/背心 400/襯衫 200)
  + 打板費(二件 4500/外套 3200/褲子 1300/三件 5800/背心 1300/襯衫 0) + 特殊自填
- **駐店師傅**：固定薪資 60000/月（含在固定成本，不論件）
- **二次試穿修改**（僅外套類項目，褲子/臀圍不適用）：肩領 300、袖子 200、腰身 250、
  袖長單手 80、袖長雙手 160、特殊自填（可複選、按次數計）

### 工資試算頁（/wage-calc，獨立於訂單系統）

手動輸入勾選、單獨計算，資料存專用 Notion 資料庫「🧮 工資試算紀錄」，完全不連訂單/派工單。

- 頂層依師傅分三分頁：**外套師傅 / 褲子師傅 / 經理**，各自的清單與結算互不相干
  （Notion 用「類型」欄位＝師傅名稱區隔，A 師傅輸入的客戶不會出現在 B 師傅頁面）
- 各師傅可選項目：
  - 外套師傅：外套（底價+扣眼+米蘭眼+雙排+票帶+劍領+半裡+全單+大衣+特殊）／修改（肩領/袖子/腰身/袖長單雙手，僅外套類）／背心（單排2200/雙排2500）
  - 褲子師傅：褲子（固定1900+特殊）／背心（固定1900）
  - 經理：品項別（二件式/三件式/外套/褲子/背心/襯衫）→ 經理費+打板費+特殊
- 每筆輸入客戶名稱後存成一筆紀錄；未結算清單可勾選多筆（含全選）→ 結算加總 → 顯示已結算區（含合計/退回/刪除）
- 各師傅獨立按月結算，同師傅同月防重複結算
- **批次計算**（經理／褲子師傅）：頁面上方可切換「單筆試算／📦批次計算」
  - 經理批次：同時輸入 二件式/三件式/外套/褲子/背心/襯衫 各幾件，自動算(經理費+打板費)×件數加總，另可加特殊項
  - 褲子師傅批次：同時輸入 褲子/背心 各幾件（皆固定1900/件），加總＋特殊項
  - 外套師傅維持單筆試算（項目多、加項組合複雜，不做批次）
  - 批次結果仍走原本存成一筆／結算流程，明細記錄各品項件數

## 月固定成本：97649

房租 10000、勞健保 13600、水費 250、電費 3000、網路 1099、瓦斯 200、記帳 3000、雜物 1500、年終 5000、固定薪資 60000。公司費攤提以 10 套/月計 = 9800（已含在工本費）。

## 派工單系統（7 種）

| 派工單 | 指派 | 內容 |
|--------|------|------|
| 📐 打版單 | 經理 | 完整樣式（量身在訂單關聯的量身記錄）|
| 🧵 毛胚製作單 | 駐店師傅/經理 | 樣式（無量身）|
| 👔 外套製作單 | 外套師傅 | 外套樣式 |
| 👖 褲子製作單 | 褲子師傅 | 褲子樣式 |
| 🦺 背心製作單 | 外套/褲子師傅 | 背心樣式 |
| ✂️ 外套修改單 | 外套師傅 | 修改項目 |
| ✂️ 褲子修改單 | 駐店師傅 | 修改項目 |

派工單直接在 Notion 派工單資料庫建記錄（不用網址生成），Notion 頁面本身就是派工單，傳連結給師傅看。師傅不應看到工資/量身以外的成本資訊。

## 製作流程

接訂單 → 訂布 → 打版 → 毛胚 → 試穿 → 開始製作 → 試穿修改 → 完成。

## 財務系統

財務總表記錄所有收支。欄位：類型(收入/支出)、分類(訂單收入/其他收入/固定成本/人事成本/材料成本/其他支出)、帳戶(🏦銀行/💵現金/💳信用卡)、金額、預估金額/實際金額/匯率差異(布料用)、關聯訂單、付款狀態、結算月份。

- 訂單成立 → 自動產生「訂單收入」記錄（權責發生制，認列全額營收）
- 訂金/尾款是現金流，跟營收分開看
- 布料成本有匯率問題：預估成本（報價）vs 實際成本（叫料後），差額記匯率差異
- 每月可一鍵產生固定成本 97649

## 本機開發

```bash
npm install
npm run dev      # 本地預覽
npm run build    # 編譯到 dist
```

需要 .env 檔（本地測試 API）：
```
NOTION_TOKEN=你的token
```

## 部署（Vercel）

GitHub repo → Vercel 匯入（自動偵測 Vite）→ 設定環境變數 NOTION_TOKEN → Deploy。

## 待辦 / 可優化

1. 工資計算 calcWages 是簡化版，未涵蓋扣眼數量、票帶、半裡、全單、大衣等加項。可改成完整解析 partInputs/partStyles。
2. 量身輸入介面店主覺得不夠直覺（想要像紙本量身單的排版）。
3. 訂單進度（流程）目前手動，可做：派工單全完成 → 自動推進訂單流程。
4. 派工單修改單的修改項目目前是空白文字，可做成勾選介面。
5. 行銷模組（社群、網頁）尚未開始。
6. 訂單查詢/管理頁、財務儀表板（月營收、利潤、趨勢）尚未做。

## 與店主溝通注意

- 一次問一個問題，給具體選項
- 重大改動前先確認方向
- 店主重視：利潤計算清楚、師傅派工方便、手機操作順暢
